# 开发日志

本文件用于记录项目每个阶段已经完成什么、接下来要做什么、哪些事情需要用户审核。

## 当前状态

- 当前阶段：阶段 13，项目数据接口
- 当前结论：阶段 13 已完成，等待用户审核
- 重要要求：本阶段只处理项目基础数据接口，不接 AI、不上传真实文件

## 阶段状态

| 阶段 | 名称 | 状态 | 说明 |
| --- | --- | --- | --- |
| 阶段 0 | 项目规则和文档框架 | 已完成 | 已创建管理文件和说明文档 |
| 阶段 1 | 可点击产品原型 | 已完成 | 已使用 Next.js、React、TypeScript 和假数据做页面流程 |
| 阶段 2 | 本地交互增强 | 已完成 | 已让转写稿、字幕、导出、删除确认等前端功能可以操作 |
| 阶段 3 | 真实音频处理接入前评审 | 已完成 | 已整理真实转写服务、隐私、费用、最小接入范围，并预留接口 |
| 阶段 4 | 本地项目管理补全 | 待用户审核 | 已让新建任务、项目列表、状态和删除在浏览器本地联动 |
| 阶段 5 | 本地文件选择预览 | 待用户审核 | 已支持选择本地文件并显示文件名、大小、格式和浏览器类型 |
| 阶段 6 | 项目详情页闭环 | 待用户审核 | 已支持每个项目打开自己的详情页 |
| 阶段 7 | 项目搜索和筛选 | 待用户审核 | 已支持工作台按关键词、类型、状态筛选项目 |
| 阶段 8 | 项目草稿独立保存 | 待用户审核 | 已支持每个项目保存自己的音频纪要或字幕草稿 |
| 阶段 9 | 导出记录管理 | 待用户审核 | 已支持导出纪要或字幕时写入本地导出记录 |
| 阶段 10 | 本地数据清理和格式校验 | 待用户审核 | 已支持文件格式不匹配提示，删除项目时同步清理本地关联数据 |
| 阶段 11 | 草稿自动保存 | 待用户审核 | 已支持编辑纪要或字幕后自动保存本地草稿 |
| 阶段 12 | Supabase 后端基础 | 待用户审核 | 已创建 Supabase 基础表和 Prisma 配置 |
| 阶段 13 | 项目数据接口 | 待用户审核 | 已新增项目 CRUD 接口，页面优先读取 Supabase |
| 最后阶段 | 真实 AI 接入 | 未开始 | 等网站功能整体完成后，再接入真实语音转文字 |

## 已完成事项

### 阶段 0

- 创建项目开发日志。
- 创建 `docs` 文件夹。
- 创建开发需求、技术方案、UI 设计规范、执行步骤、术语解释等标准文件。
- 创建 `agent.md`，记录后续工作说明和用户沟通方式。

### 阶段 1

- 创建 Next.js 项目基础结构。
- 创建首页，包含「录音生成纪要」和「视频生成字幕」两个主入口。
- 创建工作台，包含项目列表、处理中队列、最近编辑入口和导出记录。
- 创建新建任务页，用假数据模拟选择音频或视频任务。
- 创建音频纪要页，展示逐字稿、说话人、会议纪要、发言人占比和会议时间轴。
- 创建视频字幕页，展示视频预览占位、字幕列表、时间码、搜索替换入口和导出入口。
- 创建设置与隐私页，提前说明文件删除、隐私和导出格式。
- 使用白色系 UI，整体保持清爽、工具型、可读性优先。
- 使用假数据模拟完整流程，没有接入真实 AI 服务。
- 安装基础依赖并生成 `package-lock.json`。
- 通过类型检查、生产构建和安全审计。
- 根据用户审核意见，将品牌名改为「听墨」。
- 在新建任务页补充常见音频和视频格式说明。

### 阶段 2

- 音频纪要页支持本地编辑逐字稿文字。
- 音频纪要页支持修改说话人名称。
- 音频纪要页支持编辑会议摘要、关键结论和待办事项。
- 音频纪要页支持导出当前编辑内容为 `.txt` 示例纪要文件。
- 视频字幕页支持编辑字幕开始时间、结束时间和字幕文字。
- 视频字幕页支持拆分字幕块。
- 视频字幕页支持合并下一条字幕。
- 视频字幕页支持删除单条字幕。
- 视频字幕页支持搜索替换字幕文字。
- 视频字幕页支持导出 `.srt`、`.vtt`、`.txt` 示例字幕文件。
- 设置页支持删除确认演示。
- 设置页支持展开查看导出格式说明。
- 通过类型检查、生产构建和安全审计。
- 页面检查确认音频纪要、视频字幕、设置页的关键入口都能显示。

### 阶段 3

- 查阅 OpenAI 官方语音转文字资料。
- 创建 `docs/真实音频处理接入评审.md`。
- 在 `docs/技术方案.md` 中补充阶段 3 接入前评审结论。
- 创建真实接入评审页面 `/integration-review`。
- 在工作台侧边栏新增「真实接入」入口。
- 明确推荐先评估 `gpt-4o-mini-transcribe`。
- 明确 `gpt-4o-transcribe`、`gpt-4o-transcribe-diarize`、`whisper-1` 的备选定位。
- 明确第一版真实接入只做音频转文字，不做视频、说话人区分、大文件切片和自动纪要。
- 明确真实接入前必须确认文件隐私、文件大小、费用和 API Key。
- 根据用户要求，确认先不接入 AI，等网站功能整体完成后最后再接入。
- 新增 `docs/接口预留说明.md`。
- 新增 `lib/transcription-contract.ts`，记录未来转写接口的请求和返回格式。
- 新增 `GET /api/transcriptions/audio`，用于查看预留接口说明。
- 新增 `POST /api/transcriptions/audio`，当前只返回“AI 暂未接入”的占位提示，不调用任何 AI 服务。
- 在真实接入评审页补充“接口已预留，AI 暂不接入”的说明。

### 阶段 4

- 根据用户要求，继续保持不接入 AI。
- 将阶段 4 从“真实字幕和导出能力”调整为“本地项目管理补全”。
- 新增 `docs/本地项目管理说明.md`。
- 新增 `lib/local-projects.ts`，用于管理浏览器本地保存的项目数据。
- 新增 `components/DashboardClient.tsx`，让工作台读取本地项目。
- 改造新建任务页，支持填写项目名称和文件名。
- 新建任务会保存到浏览器本地，并跳转回工作台。
- 工作台可以显示新增项目。
- 工作台可以标记项目完成。
- 工作台可以删除本地项目。
- 工作台可以恢复示例项目。
- 处理中队列会根据本地项目状态变化。

### 阶段 5

- 新增 `docs/本地文件预览说明.md`。
- 新建任务页支持选择本地音频或视频文件。
- 选择文件后显示文件名、文件大小、文件格式和浏览器识别类型。
- 选择文件后自动用文件名填充项目名称。
- 创建本地任务时保存文件大小和浏览器识别类型。
- 工作台项目列表显示文件大小。
- 明确本阶段不上传文件、不读取音视频内容、不接入 AI。

### 阶段 6

- 新增 `docs/项目详情页说明.md`。
- 新增 `/projects/[id]` 项目详情页。
- 新增 `components/ProjectDetailClient.tsx`。
- 工作台项目“打开”入口会进入对应项目详情页。
- 旧的本地项目链接会自动迁移为新的项目详情页链接。
- 音频项目详情页复用音频纪要编辑器。
- 视频项目详情页复用字幕编辑器。
- 编辑器顶部会显示项目名称、文件名、格式和状态。
- 项目不存在或已删除时，显示友好提示和返回工作台按钮。
- 当前仍然使用示例转写稿和示例字幕，不接 AI。

### 阶段 7

- 新增 `docs/项目搜索筛选说明.md`。
- 工作台新增关键词搜索。
- 关键词支持匹配项目名称、文件名和文件格式。
- 工作台新增项目类型筛选：全部、音频纪要、视频字幕。
- 工作台新增项目状态筛选：全部、处理中、已完成、草稿。
- 工作台新增一键清空筛选。
- 筛选结果为空时显示友好提示。
- 顶部统计显示当前筛选结果数量。

### 阶段 8

- 新增 `docs/项目草稿保存说明.md`。
- 新增 `lib/project-drafts.ts`，用于按项目 ID 保存草稿。
- 音频纪要编辑器新增“保存草稿”。
- 音频纪要项目可保存逐字稿、发言人、会议摘要、关键结论和待办事项。
- 视频字幕编辑器新增“保存草稿”。
- 视频字幕项目可保存字幕文字、开始时间、结束时间和字幕列表。
- 再次打开同一个项目时，会自动恢复本地草稿。
- 不同项目之间的草稿互不影响。
- 当前草稿只保存在浏览器本地，不上传到服务器。

### 阶段 9

- 新增 `docs/导出记录管理说明.md`。
- 新增 `lib/export-records.ts`，用于保存本地导出记录。
- 导出音频纪要 `.txt` 时写入导出记录。
- 导出字幕 `.srt` 时写入导出记录。
- 导出字幕 `.vtt` 时写入导出记录。
- 导出字幕 `.txt` 时写入导出记录。
- 工作台导出记录改为读取浏览器本地记录。
- 工作台支持删除单条导出记录。
- 工作台支持恢复示例导出记录。
- 当前只记录导出行为，不保存导出文件副本。

### 阶段 10

- 新增 `docs/本地数据清理和格式校验说明.md`。
- 新建任务页会检查文件格式和当前任务类型是否匹配。
- 文件格式不匹配时显示友好提醒。
- 文件格式不匹配时阻止创建本地任务。
- 删除项目时会同步清理该项目的本地草稿。
- 删除项目时会同步清理该项目的本地导出记录。
- 当前只清理浏览器本地数据，不影响已经下载到电脑里的文件。

### 阶段 11

- 音频纪要编辑器支持编辑后自动保存本地草稿。
- 视频字幕编辑器支持编辑后自动保存本地草稿。
- 保留“保存草稿”按钮，用户仍然可以手动保存。
- 页面增加自动保存说明，避免用户误以为必须手动点击。
- 自动保存有短暂延迟，减少频繁保存造成的页面负担。
- 更新 `docs/项目草稿保存说明.md`，说明自动保存的使用方式和边界。
- 更新 `docs/执行步骤.md`，补充阶段 11 的目标和注意事项。
- 当前只保存到浏览器本地，不上传、不接 AI。

### 阶段 12

- 确认 Supabase MCP 已连接成功。
- 确认 Supabase 项目地址为 `https://rhkygoxxqoemioizlkja.supabase.co`。
- 确认阶段开始前 `public` 下没有业务数据表。
- 安装 `Prisma` 和 `@prisma/client`。
- 新增 `.env.example`，说明 Supabase 和 Prisma 需要的环境变量。
- 新增 `prisma/schema.prisma`，定义项目、草稿、导出记录的数据结构。
- 新增本地迁移文件，记录阶段 12 创建的数据库结构。
- 新增 `lib/db.ts`，作为后续后端接口统一连接数据库的入口。
- 选择 Prisma 6.x，保持当前项目的数据库连接配置更简单。
- 通过 Supabase MCP 创建 `projects` 表。
- 通过 Supabase MCP 创建 `project_drafts` 表。
- 通过 Supabase MCP 创建 `export_records` 表。
- 三张表都已开启 RLS 安全开关。
- 根据 Supabase 安全建议，补充加固 `set_updated_at` 数据库函数。
- Supabase 当前剩余 RLS 无策略提示为信息级，符合本阶段“不开放前端直连表”的设计。
- Supabase 当前剩余未使用索引提示为信息级，符合新建空表状态。
- 新增 `docs/Supabase后端基础说明.md`。
- 更新 `docs/技术方案.md` 和 `docs/执行步骤.md`。
- 当前只建立后端基础，不接 AI、不上传真实音视频、不做登录注册。

### 阶段 13

- 新增 `GET /api/projects`，用于读取 Supabase 项目列表。
- 新增 `POST /api/projects`，用于创建 Supabase 项目。
- 新增 `GET /api/projects/[id]`，用于读取单个 Supabase 项目。
- 新增 `PATCH /api/projects/[id]`，用于更新项目状态。
- 新增 `DELETE /api/projects/[id]`，用于删除项目。
- 新增 `lib/project-api.ts`，统一管理前端请求项目接口的方法。
- 新增 `lib/project-db-mappers.ts`，统一转换前端中文状态和数据库枚举。
- 工作台优先读取 Supabase 项目。
- 工作台在后端未配置时退回浏览器本地演示数据。
- 新建任务优先创建 Supabase 项目。
- 新建任务在后端未配置时退回浏览器本地演示项目。
- 项目详情页优先读取 Supabase 单个项目。
- 项目详情页在后端未配置时退回浏览器本地演示项目。
- 新增 `docs/项目数据接口说明.md`。
- 更新 `docs/技术方案.md` 和 `docs/执行步骤.md`。
- 当前只处理项目基础数据，草稿和导出记录后端化留到后续阶段。

## 当前待办事项

- 等待用户审核阶段 3 接入前评审。
- 等待用户审核阶段 4 本地项目管理功能。
- 等待用户审核阶段 5 本地文件选择预览。
- 等待用户审核阶段 6 项目详情页闭环。
- 等待用户审核阶段 7 项目搜索和筛选。
- 等待用户审核阶段 8 项目草稿独立保存。
- 等待用户审核阶段 9 导出记录管理。
- 等待用户审核阶段 10 本地数据清理和格式校验。
- 等待用户审核阶段 11 草稿自动保存。
- 等待用户审核阶段 12 Supabase 后端基础。
- 等待用户审核阶段 13 项目数据接口。
- 审核通过后，再继续补全其他非 AI 产品功能。
- 等网站其他功能整体完成后，再最后接入真实 AI。

## 下一步计划

后续非 AI 功能可以继续做：

- 增加草稿后端接口。
- 增加导出记录后端接口。
- 让音频纪要草稿保存到 Supabase。
- 让视频字幕草稿保存到 Supabase。

等网站功能整体完成后，如果用户确认使用 OpenAI 作为第一版真实转写服务，最后接入阶段可以做：

- 增加 `.env.example`，说明 `OPENAI_API_KEY` 怎么配置。
- 安装 OpenAI SDK。
- 新增一个只处理音频的后端接口。
- 没有 API Key 时显示友好提示。
- 上传真实音频后返回转写文字。
- 限制文件类型和大小。
- 不长期保存原始音频文件。

## 重要决定

- 项目按阶段推进，不一次性全部完成。
- 每个阶段完成后必须停下，交给用户审核。
- 第一版先做可点击原型，不直接接真实 AI 转写。
- 所有说明尽量使用中文和大白话。
- 用户是计算机小白，不能默认用户理解专业概念。
- 阶段 1 使用 Next.js、React、TypeScript、普通 CSS 和假数据。
- 阶段 2 只做本地浏览器交互，不保存到真实后台。
- 阶段 3 只完成接入前评审，不直接调用付费服务。
- 阶段 3 已预留接口，但接口只返回占位提示，不调用 AI。
- 阶段 4 使用浏览器本地存储，不使用真实数据库。
- 阶段 5 只读取本地文件基本信息，不上传文件。
- 阶段 6 只打通项目详情页，不生成真实转写或字幕。
- 阶段 7 只搜索本地项目元信息，不搜索真实文件内容。
- 阶段 8 只保存本地编辑草稿，不上传到服务器。
- 阶段 9 只保存本地导出记录，不保存导出文件副本。
- 阶段 10 只清理浏览器本地数据，不删除电脑里的真实文件。
- 阶段 11 只自动保存浏览器本地草稿，不上传到服务器。
- 阶段 12 使用 Supabase 建立后端基础，但不接 AI、不上传真实文件。
- 阶段 13 只接入项目基础数据接口，草稿和导出记录仍先保留本地逻辑。
- 真实语音识别、说话人区分、视频处理和文件存储要单独确认后再实现。
